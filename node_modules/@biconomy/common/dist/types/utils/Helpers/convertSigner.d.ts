import { SupportedSigner } from "../Types.js";
import { SmartAccountSigner } from "@alchemy/aa-core";
interface SmartAccountResult {
    signer: SmartAccountSigner;
    chainId: number | null;
    rpcUrl: string | null;
}
export declare const convertSigner: (signer: SupportedSigner, skipChainIdCalls?: boolean) => Promise<SmartAccountResult>;
export {};
//# sourceMappingURL=convertSigner.d.ts.map